# WAF-Project
FireWAF is a production-grade, self-hosted Web Application Firewall (WAF) built from scratch in TypeScript, designed as a transparent reverse proxy that sits between clients and backend web applications to inspect every incoming HTTP request in real time. 
